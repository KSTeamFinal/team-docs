# 계약서 분석 AI 프로그램 로직 설계

## 1. 문서 목적

이 문서는 계약서 분석 AI 시스템의 프로그램 로직을 대략적인 흐름 기준으로 정리한 문서다.

## 2. 전체 로직 흐름

```text
계약서 업로드
-> 문서 형식 판별
-> OCR 및 이미지 탐지
-> 텍스트 전처리
-> 조항 분리
-> 핵심 정보 추출
-> 위험 조항 분석
-> 요약 및 질의응답
-> 결과 저장
-> 결과 시각화
```

## 3. 단계별 로직

### 3-1. 사용자 입력 단계

사용자가 React 기반 웹 화면에서 계약서를 업로드한다. 입력 형식은 텍스트, PDF, 이미지다.

### 3-2. 문서 판별 및 전처리 단계

FastAPI 서버가 파일 형식을 판별한다. 텍스트 문서는 직접 파싱하고, PDF 및 이미지 문서는 PaddleOCR을 이용하여 텍스트를 추출한다. 이후 줄바꿈, 공백, 특수문자, 조항 형식을 정리한다.

### 3-3. 이미지 객체 탐지 단계

스캔 문서나 이미지 문서에 대해 YOLOv8 모델을 실행하여 인감, 서명, 체크 표식 등의 시각 요소를 탐지한다. 탐지 결과는 위치 좌표와 함께 저장한다.

### 3-4. 조항 분리 단계

전처리된 텍스트를 기준으로 `제1조`, `제2조`와 같은 패턴을 인식하여 문서를 조항 단위로 분리한다.

### 3-5. 핵심 정보 추출 단계

정규표현식으로 날짜, 금액, 계약 기간 등 정형 정보를 1차 추출하고, LangChain을 통해 Gemini API에 분석 요청을 보내 계약 유형 분류와 핵심 정보 보정을 수행한다.

### 3-6. 위험 조항 분석 단계

정규표현식 및 룰 기반 탐지로 자동 갱신, 일방 해지, 과도한 손해배상, 모호한 지급 조건 등을 찾고, Gemini API를 통해 사용자 친화적인 설명 문장을 생성한다.

### 3-7. 요약 및 질의응답 단계

계약서 전체 내용을 기반으로 요약을 생성한다. 사용자가 질문을 입력하면 LangChain이 관련 조항을 검색하고, Gemini API가 근거 기반 답변을 생성한다.

### 3-8. 결과 저장 및 제공 단계

분석 결과, 조항 데이터, 위험 조항, 탐지 결과, 질의응답 로그를 MySQL에 저장하고, 프론트엔드는 이를 시각화하여 제공한다.
