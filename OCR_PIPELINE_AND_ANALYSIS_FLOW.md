# OCR 파이프라인 및 분석 흐름 설계 결정 사항

## 1. 문서 목적

이 문서는 2026-04-05 개발 논의에서 결정된 OCR 도구 선택, 분석 흐름 설계, 기술 결정 사항을 정리한 문서다.

---

## 2. 환경 설정 완료 사항

### 2-1. 설치된 도구

| 항목 | 버전 | 비고 |
|------|------|------|
| Python | 3.11.9 | `C:\Users\...\AppData\Local\Programs\Python\Python311` |
| Node.js | 24.14.1 LTS | |
| MySQL | 8.4.8 | 서비스명 `MySQL84`, 자동 시작 |
| CUDA Toolkit | 12.8 | RTX 5080 대응 |
| PyTorch | 2.11.0+cu128 | CUDA 12.8 GPU 버전 |

### 2-2. DB 설정

- DB명: `clair_db`
- root 비밀번호: 별도 공유
- `.env` 파일: `clair-backend/.env` 생성 완료

### 2-3. 서버 실행 확인

- `clair-backend`: http://localhost:8000 (정상)
- `clair-ai`: http://localhost:8001 (정상)

---

## 3. OCR 도구 검토 및 결정

### 3-1. 검토한 도구

| 도구 | 한글 인식 | M2 무게 | 문서 구조 파싱 | 비고 |
|------|-----------|---------|--------------|------|
| PaddleOCR | 양호 | 무거움 | ❌ | Windows CPU 3.x 버그 있음, 2.8.1 사용 중 |
| EasyOCR | 보통 | 가벼움 | ❌ | 구조 파싱 없어 교체 효과 낮음 |
| Tesseract | 보통 | 매우 가벼움 | ❌ | 한글 인식 낮음 |
| **Docling** | **텍스트 PDF: 완벽 / 스캔: 양호** | **중간** | **✅** | IBM 오픈소스, 2024년 10월 공개 |
| olmOCR | 우수 | 매우 무거움 | ✅ | AllenAI, 2025년 2월 공개, 7B VLM 기반 |

### 3-2. 결정: Docling + RapidOCR 백엔드

**선택 이유:**
- 디지털 PDF(텍스트 레이어 존재)는 OCR 없이 구조 파싱 가능 → 한글 인식 문제 없음
- 스캔 PDF/이미지는 RapidOCR 백엔드 사용 (내부적으로 PaddleOCR v5 기반)
- 조항 구조(제목/본문 경계)를 처음부터 분리해줌 → RAG 입력 품질 향상

**파일 형태별 처리 분기:**
```
업로드된 파일
  ├── 디지털 PDF / DOCX  →  Docling 텍스트 레이어 직접 파싱
  └── 스캔 PDF / 이미지  →  Docling + RapidOCR 백엔드 (OCR 후 구조 파싱)
```

> **현재 상태:** OCRPipeline은 PaddleOCR 2.8.1 기반으로 동작 중. Docling 교체 작업은 미진행.

---

## 4. 분석 흐름 확정

### 4-1. 전체 흐름

```
계약서 업로드 (이미지 or PDF)
  → OCR + 문서 구조 파싱 (Docling)
  → OCR 품질 보정 (Gemini Vision, 조건부)
  → 조항 단위 분리
  → RAG: AIHUB 노동법 데이터와 조항 단위 비교 분석
  → 위반/문제 조항 추출
  → 대시보드 형태로 제공
```

### 4-2. 기존 흐름과 차이

기존 `CONTRACT_AI_PROGRAM_LOGIC.md`에 기술된 흐름에서 아래 부분이 변경/구체화됐다.

| 기존 | 변경 후 |
|------|---------|
| PaddleOCR로 텍스트 추출 후 정규표현식으로 조항 분리 | Docling이 OCR + 조항 구조 파싱을 함께 처리 |
| 룰 기반 위험 조항 탐지 | AIHUB 노동법 데이터셋 기반 RAG 비교로 법령 위반 탐지 |

---

## 5. OCR 품질 보정 전략

스캔 문서의 경우 OCR 오인식이 발생할 수 있다. confidence score 기반으로 Gemini Vision 보정을 조건부 실행한다.

```
OCR 실행
  → 페이지별 confidence score 계산
  ├── 평균 > 0.85  →  그대로 사용
  └── 평균 ≤ 0.85  →  Gemini Vision 보정 요청
                        입력: 원본 이미지 + OCR 텍스트
                        출력: 수정된 텍스트 (구조 유지)
```

**미결정 사항:** 보정 단위를 페이지별로 할지, confidence 낮은 페이지만 할지, 전체 문서를 한 번에 넘길지 추후 결정 필요.

---

## 6. RAG 파이프라인 설계

### 6-1. 노동법 데이터셋

- 출처: **AIHUB** (관련 법령 데이터셋 확인 완료)
- 형태: CSV
- 사전 작업 (1회): CSV 청킹 → 임베딩 → 벡터 DB 저장

### 6-2. 파이프라인 흐름

```
[사전 작업 - 1회]
AIHUB 법령 CSV
  → 조항 단위 청킹
  → 임베딩 (Gemini Embedding 또는 한국어 특화 모델)
  → 벡터 DB 저장 (ChromaDB 또는 pgvector)

[계약서 분석 - 매 요청]
계약서 조항 텍스트
  → 벡터 DB에서 관련 법령 검색
  → Gemini: 해당 조항이 법령을 위반하는지 판단
  → 위반 여부 + 근거 법령 추출
  → 대시보드 전달
```

> **미결정 사항:** AIHUB 데이터셋 컬럼 구조 확인 후 청킹 전략 결정 필요.

---

## 7. Gemini API 비용 추정

개발 테스트 기준 (월 100개 계약서 처리 가정)

| 모델 | 월 예상 비용 (한화) | 비고 |
|------|-----------------|------|
| Gemini 2.0 Flash | ~1,700원 | 개발 테스트 권장 |
| Gemini 1.5 Pro | ~20,000원 | |
| Gemini 2.5 Pro | ~28,000원 | |

**무료 티어:** Gemini 2.0 Flash 기준 하루 1,500 요청 무료. 개발 초반에는 무료 티어 내에서 대부분 해결 가능.

---

## 8. 미결정 / 추후 진행 사항

- [ ] OCRPipeline을 Docling + RapidOCR 백엔드로 교체
- [ ] AIHUB 데이터셋 컬럼 구조 확인 및 청킹 전략 결정
- [ ] 벡터 DB 선택 (ChromaDB vs pgvector)
- [ ] OCR 보정 단위 결정 (페이지별 / 조건부 / 전체)
- [ ] Gemini API 키 서버 `.env`에 등록
