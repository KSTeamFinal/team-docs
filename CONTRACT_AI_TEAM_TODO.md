# 계약서 분석 AI 4인 역할별 Todo 문서

## 1. 문서 목적

이 문서는 계약서 분석 AI 프로젝트를 4명이 1개월 안에 시연 가능한 수준으로 완성하기 위한 역할 분담과 주차별 Todo를 정리한다. 기준 아키텍처는 `React + PWA + FastAPI + MySQL + Gemini API + LangChain + PaddleOCR + YOLOv8`이다.

## 2. 팀 역할 정의

### 1번. 프론트엔드 담당

- React 기반 화면 구현
- PWA 설정
- 업로드, 문서 목록, 결과 상세, Q&A UI 개발

### 2번. 백엔드/API 담당

- FastAPI 서버 구조
- 문서 등록/조회 API
- MySQL 저장 구조
- 파일 업로드 처리
- 작업 상태 관리

### 3번. AI 파이프라인 담당

- Gemini API 연동
- LangChain 체인 설계
- 구조화 출력 스키마 설계
- 요약, 분류, Q&A 프롬프트 설계

### 4번. 파서/룰/테스트 담당

- PaddleOCR 처리 흐름 점검
- YOLOv8 데이터셋 관리 보조
- 조항 분리 및 정규표현식 추출
- 리스크 룰 설계 및 테스트셋 관리

## 3. 공통 우선순위

1. 업로드부터 결과 조회까지 한 번은 끝까지 도는 흐름을 먼저 만든다.
2. 정확도 개선보다 시연 안정성을 먼저 확보한다.
3. 지원 계약 유형은 NDA, 용역계약서, 근로계약서로 제한한다.
4. 텍스트 분석과 이미지 탐지 결과를 모두 화면에서 확인 가능하게 만든다.

## 4. 주차별 Todo

### 1주차

#### 1번 프론트엔드

- React 프로젝트 초기 세팅
- PWA 설정
- 업로드 화면 초안 구현
- 문서 목록 페이지 뼈대 구현

#### 2번 백엔드/API

- FastAPI 프로젝트 초기 세팅
- Dockerfile 구성
- `POST /documents`, `GET /documents/{id}` 초안 구현
- MySQL 기본 스키마 정의

#### 3번 AI 파이프라인

- Gemini API 연결 테스트
- LangChain 기본 체인 연결 테스트
- 구조화 출력용 JSON 스키마 초안 작성
- 계약 유형 분류 프롬프트 초안 작성

#### 4번 파서/룰/테스트

- PaddleOCR 실행 테스트
- YOLOv8/Roboflow 데이터 확인
- 조항 분리 정규식 초안 작성
- 샘플 계약서 3종 수집 및 정리

### 2주차

#### 1번 프론트엔드

- 업로드 후 분석 요청 흐름 연결
- 문서 상세 화면 레이아웃 구현
- 핵심 필드 카드 UI 구현
- 분석 상태 UI 구현

#### 2번 백엔드/API

- `POST /documents/{id}/upload`
- `POST /documents/{id}/analyze`
- `GET /documents/{id}/extractions`
- MySQL 저장 로직 구현

#### 3번 AI 파이프라인

- 계약 유형 분류 연결
- 핵심 필드 추출 연결
- 추출 실패 시 `null + reason` 규칙 적용
- 요약 프롬프트 작성

#### 4번 파서/룰/테스트

- OCR 결과 정제 로직 구현
- 조항 분리 로직 구현
- 필드 후보 추출 로직 구현
- 파서 테스트 작성

### 3주차

#### 1번 프론트엔드

- 위험 조항 리스트 UI 구현
- 조항 하이라이트 UI 구현
- 탐지 결과 표시 UI 구현
- Q&A 입력 및 답변 영역 구현

#### 2번 백엔드/API

- `GET /documents/{id}/risks`
- `GET /documents/{id}/clauses`
- `GET /documents/{id}/detections`
- `POST /documents/{id}/qa`

#### 3번 AI 파이프라인

- 요약 기능 연결
- Q&A 기능 연결
- 근거 조항 포함 응답 형식 고정
- LangChain 기반 근거 검색 흐름 정리

#### 4번 파서/룰/테스트

- YOLOv8 탐지 결과 검증
- 리스크 룰 구현
- 테스트 계약서 3종 회귀 테스트 수행
- 탐지 근거 문자열 저장 검증

### 4주차

#### 1번 프론트엔드

- 모바일/노트북 화면 점검
- 예외 메시지 정리
- 시연용 UI 흐름 마감

#### 2번 백엔드/API

- 예외 처리 보강
- 요청/응답 로깅 정리
- 배포 환경 변수 정리

#### 3번 AI 파이프라인

- 프롬프트 튜닝
- 응답 길이/형식 안정화
- 오답 및 누락 사례 보정

#### 4번 파서/룰/테스트

- 최종 테스트 시트 작성
- OCR 및 탐지 실패 사례 정리
- 데모 계약서 정리

## 5. 시연 직전 체크리스트

1. 대표 계약서 3종이 정상 분석된다.
2. 핵심 정보 추출 결과가 화면에 표시된다.
3. 위험 조항 1개 이상 탐지된다.
4. 인감 또는 서명 탐지 결과가 표시된다.
5. 질문 3개 이상에 대해 근거 조항 포함 답변이 가능하다.
